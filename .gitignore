*.iml
.gradle
.idea/
.DS_Store
build/
captures/
.externalNativeBuild
.cxx
local.properties

# Release signing — never commit the upload key or its passwords
keystore.properties
*.jks
*.keystore

# CodeGraph local index (tool artifact)
.codegraph/
